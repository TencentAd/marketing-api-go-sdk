/*
 * Marketing API
 *
 * Marketing API
 *
 * API version: 1.3
 * Generated by: Swagger Codegen (https://github.com/swagger-api/swagger-codegen.git)
 */

package main

import (
	"encoding/json"
	"fmt"
	"net/http"

	"github.com/tencentad/marketing-api-go-sdk/pkg/ads"
	"github.com/tencentad/marketing-api-go-sdk/pkg/config"
	"github.com/tencentad/marketing-api-go-sdk/pkg/errors"
	"github.com/tencentad/marketing-api-go-sdk/pkg/model"
)

type BatchOperationAddExample struct {
	TAds        *ads.SDKClient
	AccessToken string
	Data        model.BatchOperationAddRequest
}

func (e *BatchOperationAddExample) Init() {
	e.AccessToken = "YOUR ACCESS TOKEN"
	e.TAds = ads.Init(&config.SDKConfig{
		AccessToken: e.AccessToken,
		IsDebug:     true,
	})
	e.Data = model.BatchOperationAddRequest{
		AccountId:      int64(0),
		OperationType:  model.BatchOperationType_DATE,
		ResourceIdList: &[]int64{int64(0)},
		OperationSpec: &model.OperationSpec{
			DateSpec: &model.DateSpec{
				BeginDate: "YOUR ADGROUP BEGIN DATE",
				EndDate:   "YOUR ADGROUP END DATE",
			},
		},
		ResourceType: model.ResourceType_RESOURCE_TYPE_ADGROUP,
	}
}

func (e *BatchOperationAddExample) RunExample() (interface{}, http.Header, error) {
	tads := e.TAds
	// change ctx as needed
	ctx := *tads.Ctx
	return tads.BatchOperation().Add(ctx, e.Data)
}

func main() {
	e := &BatchOperationAddExample{}
	e.Init()
	response, headers, err := e.RunExample()
	if err != nil {
		if resErr, ok := err.(errors.ResponseError); ok {
			errStr, _ := json.Marshal(resErr)
			fmt.Println("Response error:", string(errStr))
		} else {
			fmt.Println("Error:", err)
		}
	}
	fmt.Println("Response data:", response)
	fmt.Println("Headers:", headers)
}
